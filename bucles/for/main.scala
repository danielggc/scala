
object fords {
    def main(args:Array[String]){
        val numerosPrimos:List[Int] = List(1,3,5,7,11,13,2,4,5,6,8,12);
        for(i <- numerosPrimos if i %2 !=0){
            println(s"nuemro primo ${i}");
        }
        var i:Int=0
        val to:Int=5
        for ( i <to;  i++) {
            if (max < arr[i+1])
            max = arr[i+1];
        }       
    }
}